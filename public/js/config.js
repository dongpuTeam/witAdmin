

var serverURL = 'https://wx.ccepc.com/api/';

var config = {

    // 微信登录
    login: `${serverURL}login`,

    //工厂列表
    factorylist: `${serverURL}common/getFactoryList`

  
}

module.exports = config